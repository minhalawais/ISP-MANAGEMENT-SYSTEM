"use client"

/**
 * Customer forms need a wider shell; reuse the shared Modal with xl size.
 * Kept as a thin re-export so existing customerCrudPage imports keep working.
 */
export { Modal } from "./modal.tsx"
